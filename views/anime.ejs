<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/css/index.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p><%= count%>页</p>
    <br />
    
    <% filesArr.map(filename=>{
    %>
      <h3><%= filename %></h3>
    
    <% if (['jpg','jpeg','png','bmp','gif'].includes(getdir(filename))) {
    %>
      <img src="../resources/<%= title + '/' +filename %>"/>
    <%} else if (['mp3'].includes(getdir(filename))){ %>
      <audio controls="controls">
        <source src="../resources/<%= title + '/' +filename %>" type="audio/mp3" />
      </audio>
    <%} else if (['mp4','m4v'].includes(getdir(filename))){ %>
      <video width="100%" height="100%" controls>
        <source src="../resources/<%= title + '/' +filename %>" type="video/mp4">
      </video>
    <%} 
    })%>
    
  </body>
</html>